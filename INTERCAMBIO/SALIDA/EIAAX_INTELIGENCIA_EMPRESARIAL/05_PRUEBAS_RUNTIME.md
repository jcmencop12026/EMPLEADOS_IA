# 05 — Pruebas runtime

```bash
python3 -m pytest tests/test_inteligencia_empresarial_evolution.py -q
python3 -m pytest tests/test_arquitecto_transformacion.py tests/test_bloque_producto_1_evaluacion.py -q
```

**25 passed** (9 IE + 8 arquitecto + 8 BP1)

Cubre: plan adaptativo, suficiencia dossier, evaluación por nivel, cadena analítica, motor proactivo, multitenant, contratos, escenarios ampliados, regresión BP1.

```bash
cd frontend && npm run build
```

**PASS**
